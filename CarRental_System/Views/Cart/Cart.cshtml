@*@using CarRental_System.Models
@model List<Cart>
@{
    ViewBag.Title = "Cart";
    Layout = "~/Views/Shared/_LayoutCarRental.cshtml";
}

<table align="center" border="1">
    <tr style="text-align:center; font-weight:bold">
        <td> Car ID </td>
        <td> Car name </td>
        <td> photo </td>
        <td> Quantity </td>
        <td> Unti Price </td>
        <td> Into money </td>
        <td width="50"></td>
        <td width="50"></td>
        <td width="50"></td>
    </tr>
    @foreach (var item in Model)
    {
        <tr style="text-align:center; font-weight:bold">
            <td> @item.iCarID </td>
            <td> @item.sCarname </td>
            <td> <img src="~/Picture/@item.sPicture" /> </td>
            @using (Html.BeginForm("UpdateCart", "Cart", new { @iProID = item.iCarID }))
            {
                <td>
                    <input type="number" min="1" name="txtSoluong" value="@item.iQuantity" style="background-color:yellow" />
                </td>
                <td> @String.Format("{0:0,0}", item.dUnitPrice) </td>
                <td> @String.Format("{0:0,0}", item.dInto_money) </td>
                <td> <a href="@Url.Action("Details", "Rentalcar", new { id = item.iCarID })"> Detail </a></td>
                <td> @Html.ActionLink("Delete", "DeleteCart", new { @iProID = item.iCarID })</td>
                <td> <input type="submit" value="Update" /></td>
            }
        </tr>
    }
    <tr style="font-weight:bold; text-align:right; color:red">
        <td colspan="4"> Quantity of cars: @ViewBag.Total</td>
        <td colspan="5"> Total: @String.Format("{0:0,0}", ViewBag.TotalMoney) USD </td>
    </tr>

    <tr style="font-weight:bold; color:blue; text-align:right">
        <td colspan="9">
            @Html.ActionLink("Remove All Cart", "DeleteAllCart")
        </td>
    </tr>
    <tr style="font-weight:bold; color:blue; text-align:right">
        <td colspan="9" align="center">
            <button style="border-radius:5">@Html.ActionLink("Confirm", "Order", "Cart");</button>
        </td>
    </tr>


</table>*@
